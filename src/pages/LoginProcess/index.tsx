import LoginProcessing from "../../components/LoginProcessing";

const LoginProcess = () => {
    return (
        <>
            <LoginProcessing />
        </>
    );
};

export default LoginProcess;
